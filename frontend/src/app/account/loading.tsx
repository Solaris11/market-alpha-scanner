import { RouteLoadingSkeleton } from "@/components/terminal/RouteLoadingSkeleton";

export default function Loading() {
  return <RouteLoadingSkeleton metricCount={4} title="Loading account operating context" />;
}
