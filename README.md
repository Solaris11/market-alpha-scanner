# Market Alpha Scanner

## API

To run the API locally for development:
```bash
uvicorn api.main:app --reload
```

To run the API using Docker:
```bash
docker compose up -d market-alpha-api
```

The API documentation is available at `/docs` on the running server (e.g., `http://localhost:18000/docs`).
